---
apiVersion: v1
kind: Secret
metadata:
  name: spark-secret
  namespace: spark
type: Opaque
stringData:
  # Add any sensitive configuration here
  # For GCS, we use Workload Identity, so no keys needed
  CASSANDRA_USERNAME: "cassandra"
  CASSANDRA_PASSWORD: "cassandra"
